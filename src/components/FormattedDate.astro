---
import { formatToLocaleDateString } from "../utils/formatDates";

type Props = {
  date: Date;
  classes?: string;
};

const { date, classes } = Astro.props;
---

<time datetime={date.toISOString()} class={classes}>
  {formatToLocaleDateString(date)}
</time>
